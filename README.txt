to boot up the local server just run `python main.py` 

to commit changes to the google cloud server, run `/Users/samlefforge/google-cloud-sdk/bin/gcloud app deploy`
where you replace the start with the path to your own google cloud sdk